plugins {
    id("loom")
}

version = "0.0.0"
group = "user11681"

loom {
    minecraftVersion = "1.16.4"
}

dependencies {
    modInclude("api-base")
    modInclude("api-resource-loader:0.3.1+")

    bloatedInclude("auto-config")
    bloatedInclude("cloth-config:4.+")

    intransitive("mod-menu:1.14.6+")
}
